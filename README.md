# Diagramme de classes – Collège « Les Sardines »

Dépôt des tp

---

## Visualiser le diagramme Mermaid

### Solution 1 — Via une extension dans l’IDE (recommandé)

Si tu utilises **VS Code** :

1. Installer l’extension **Markdown Preview Mermaid Support**
   - ou **Mermaid Markdown Syntax Highlighting**
2. Ouvrir le fichier contenant le diagramme (`.md`)
3. Activer l’aperçu Markdown  
   - `Ctrl + Shift + V` (Windows/Linux)  
   - `Cmd + Shift + V` (Mac)

👉 Le diagramme s’affiche automatiquement.

---

### Solution 2 — Via le site Mermaid Live

1. Aller sur 👉 https://mermaid.live/
2. Copier le code Mermaid du diagramme
3. Coller le code dans l’éditeur (partie gauche)
4. Le diagramme s’affiche instantanément à droite
5. Possibilité d’exporter en **PNG**, **SVG** ou **PDF**

---

### Solution 3 — Via GitHub / GitLab

- GitHub et GitLab **rendent automatiquement** les diagrammes Mermaid dans les fichiers Markdown (`README.md`)
- Aucun outil supplémentaire n’est nécessaire

---

## Format

- **Langage** : Mermaid
- **Type** : `classDiagram`

---
